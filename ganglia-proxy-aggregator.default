#!/bin/sh
#

# Default: empty (take the clustername from the first node to contact).
# CLUSTER_NAME="test"

#
# Hosts to aggregate / proxy:
# eg. HOSTS="node01:8649 node02 nodeN:8649"
HOSTS="localhost:8649"
